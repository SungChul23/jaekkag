apiVersion: v1
kind: Service
metadata:
  name: inventory-worker
  namespace: ecommerce
  labels:
    app: inventory-worker
spec:
  selector:
    app: inventory-worker
  ports:
    - name: metrics
      port: 8002
      targetPort: 8002