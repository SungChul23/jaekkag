apiVersion: v1
kind: Service
metadata:
  name: order-api-service
  namespace: ecommerce
  labels:
    app: order-api

spec:
  type: ClusterIP

  selector:
    app: order-api

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000