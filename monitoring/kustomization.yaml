apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Order API, Publisher, Worker의 Prometheus 수집 설정
  - prometheus/service-monitors.yaml

  # Prometheus 장애 감지 규칙
  - prometheus/alert-rules.yaml

  # 이커머스 통합 Grafana 대시보드
  - grafana/ecommerce-dashboard.yaml